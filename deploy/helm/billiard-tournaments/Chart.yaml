apiVersion: v2
name: billiard-tournaments
description: Billiard Tournament Manager — full-stack app with Node/Express backend, React frontend, and PostgreSQL
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "16.4.1"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
